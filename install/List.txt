brew install tree
brew install wget

# development tools
brew install node
brew install tmux
brew install z
brew install zsh
brew install zsh-syntax-highlighting
brew install zsh-autosuggestions

#upgrading packages

brew upgrade npm
npm install -g grunt-cli
